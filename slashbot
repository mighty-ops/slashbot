#!/usr/bin/env node

var slashbot = require('slashbot');

app.set('port', (process.env.PORT || 5000));

slashbot({
  name : 'slashbot',
  responders : [
    './responders/shout',
    './responders/lmgtfy'
  ]
},function(err){
  if(err) throw err
  // your bot is now started.
})
